<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:custom="http://schemas.android.com/apk/res-auto"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    tools:context=".HomeActivity" >

    <!-- TextView
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:text="@string/hello_world" /-->
    <!--view
        class="org.ricts.abstractmachine.ui.device.PinView"
        android:id="@+id/pin"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        custom:position="right"
        custom:orientation="horizontal"
        /-->

    <view
        class="org.ricts.abstractmachine.ui.RightAngleTriangleView"
        android:id="@+id/device_edge"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        custom:pinOrientation="vertical"
        custom:fillPosition="right"
        custom:diagonal="topRightToBottomLeft"
        custom:fillColour="@color/test_color2"
        />
</RelativeLayout>